spring.application.name=API-GATEWAY
server.port=8765

# Config Server URL
spring.config.import=configserver:http://localhost:9093

# Eureka client configuration
eureka.instance.prefer-ip-address=true
eureka.instance.hostname=localhost
eureka.client.fetch-registry=true
eureka.client.register-with-eureka=true
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# Disable default discovery locator (we use manual routes in GatewayConfig)
spring.cloud.gateway.discovery.locator.enabled=false
spring.cloud.gateway.discovery.locator.lower-case-service-id=true

# JWT secret (must be >= 256 bits for HS256)
jwt.secret=26c4cbf903245338881de345222de9e59082fbfd9fd4dd1b198dd734a53d89d0
